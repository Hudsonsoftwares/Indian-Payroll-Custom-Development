<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- India-specific components.
             Reads from contract/version salary breakdown (contract.hra / contract.da / contract.fixed_allowance)
             with automatic fallback to company policy percentages of BASIC if not set. -->
        <record id="hr_rule_hra_india" model="hr.salary.rule">
            <field name="name">House Rent Allowance</field>
            <field name="code">HRA</field>
            <field name="sequence">15</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[
nominal = contract.hra or 0.0
if contract.pay_by_attendance and nominal > 0.0:
    work_line = worked_days.WORK100
    earned_hours = work_line.number_of_hours if work_line else 0.0
    sched_data = payslip._get_attendance_vs_schedule(contract, payslip.date_from, payslip.date_to)
    sched_hours = sched_data.get('scheduled_hours', 0.0)
    result = round(nominal * (earned_hours / sched_hours), 2) if sched_hours > 0.0 else 0.0
else:
    result = nominal
]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_da_india" model="hr.salary.rule">
            <field name="name">Dearness Allowance</field>
            <field name="code">DA</field>
            <field name="sequence">16</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[
nominal = contract.da or 0.0
if contract.pay_by_attendance and nominal > 0.0:
    work_line = worked_days.WORK100
    earned_hours = work_line.number_of_hours if work_line else 0.0
    sched_data = payslip._get_attendance_vs_schedule(contract, payslip.date_from, payslip.date_to)
    sched_hours = sched_data.get('scheduled_hours', 0.0)
    result = round(nominal * (earned_hours / sched_hours), 2) if sched_hours > 0.0 else 0.0
else:
    result = nominal
]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_fixed_allowance_india" model="hr.salary.rule">
            <field name="name">Fixed Allowance</field>
            <field name="code">FIXED</field>
            <field name="sequence">17</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[
nominal = contract.fixed_allowance or 0.0
if contract.pay_by_attendance and nominal > 0.0:
    work_line = worked_days.WORK100
    earned_hours = work_line.number_of_hours if work_line else 0.0
    sched_data = payslip._get_attendance_vs_schedule(contract, payslip.date_from, payslip.date_to)
    sched_hours = sched_data.get('scheduled_hours', 0.0)
    result = round(nominal * (earned_hours / sched_hours), 2) if sched_hours > 0.0 else 0.0
else:
    result = nominal
]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_standard_allowance_india" model="hr.salary.rule">
            <field name="name">Standard Allowance</field>
            <field name="code">STD_ALW</field>
            <field name="sequence">18</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[
nominal = contract.standard_allowance or 0.0
if contract.pay_by_attendance and nominal > 0.0:
    work_line = worked_days.WORK100
    earned_hours = work_line.number_of_hours if work_line else 0.0
    sched_data = payslip._get_attendance_vs_schedule(contract, payslip.date_from, payslip.date_to)
    sched_hours = sched_data.get('scheduled_hours', 0.0)
    result = round(nominal * (earned_hours / sched_hours), 2) if sched_hours > 0.0 else 0.0
else:
    result = nominal
]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_performance_bonus_india" model="hr.salary.rule">
            <field name="name">Performance Bonus</field>
            <field name="code">PERF_BONUS</field>
            <field name="sequence">19</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[result = payslip_record.hds_in_compute_performance_bonus() if payslip_record else payslip.hds_in_compute_performance_bonus()]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_retention_bonus_india" model="hr.salary.rule">
            <field name="name">Retention Bonus</field>
            <field name="code">RETENTION_BONUS</field>
            <field name="sequence">19</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[result = payslip_record.hds_in_compute_retention_bonus() if payslip_record else payslip.hds_in_compute_retention_bonus()]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

        <record id="hr_rule_lta_india" model="hr.salary.rule">
            <field name="name">Leave Travel Allowance</field>
            <field name="code">LTA</field>
            <field name="sequence">20</field>
            <field name="category_id" ref="hudson_payroll_base.rule_category_alw"/>
            <field name="struct_id" ref="hudson_payroll_base.structure_base"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute"><![CDATA[result = contract.lta_allowance or 0.0]]></field>
            <field name="appears_on_payslip" eval="True"/>
        </record>

    </data>
</odoo>
